{
  "name": "quran-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "clean-build": "tsc --build --clean && rimraf dist && npm run build",
    "test": "vitest",
    "clean-test": "tsc --build --clean && vitest --clearCache && npm run test"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  }
}
